module.exports = { extends: [ '<%= eslintType === 'index' ? require.resolve('eslint-rules-config') :
require.resolve(`eslint-rules-config/${eslintType}`) %>', <%_ if (enablePrettier) { _%> 'prettier', <%_ } _%> ], };
