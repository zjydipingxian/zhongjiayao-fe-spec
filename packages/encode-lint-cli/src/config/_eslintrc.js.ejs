<% let configExtends = [ require.resolve(eslintType === 'index' ? "eslint-config-code" : `eslint-config-code/${eslintType}`) ]; %>
<% if (enablePrettier) { %> <% configExtends.push('prettier'); %> <% } %> module.exports = { extends: <%-
JSON.stringify(configExtends) %> };
