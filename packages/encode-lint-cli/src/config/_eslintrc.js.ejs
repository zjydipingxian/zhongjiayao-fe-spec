<% let configExtends = [
  require.resolve(eslintType === 'index' ? "eslint-rules-config" : `eslint-rules-config/${eslintType}`)
]; %>
<% if (enablePrettier) { %>
  <% configExtends.push('prettier'); %>
<% } %>
module.exports = {
  extends: <%- JSON.stringify(configExtends) %>
};


